---
apiVersion: v1
kind: ConfigMap
metadata:
  namespace: ovn-kubernetes
  name: ovnkubernetes-config
data:
  controller-config.yaml: |-
{{.NetworkControllerConfig | indent 4}}

---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: ovn-controller
  namespace: ovn-kubernetes
  annotations:
    kubernetes.io/description: |
      This deployment runs the ovn-kubernetes networking controller.
  labels:
    app: ovn-kubernetes
spec:
  selector:
    matchLabels:
      app: ovn-kubernetes
  template:
    metadata:
      labels:
        app: ovn-kubernetes
    spec:
      containers:
      - name: ovn-controller
        image: {{.HypershiftImage}}
        command: ["hypershift", "openshift-network-controller"]
        args:
        - "--config=/config/controller-config.yaml"
        resources:
          requests:
            cpu: 10m
            memory: 50Mi
        volumeMounts:
        - mountPath: /config
          name: config
          readOnly: true
        env:
        # TODO: bind-mount the kubelet's kubeconfig like the openshift-sdn process
        # need to get the `oc` binary in to the hypershift image first
        - name: KUBERNETES_SERVICE_PORT # allows the controller to communicate with apiserver directly on same host.
          value: "{{.KUBERNETES_SERVICE_PORT}}"
        - name: KUBERNETES_SERVICE_HOST # allows the controller to communicate with apiserver directly on same host.
          value: "{{.KUBERNETES_SERVICE_HOST}}"
      hostNetwork: true
      nodeSelector:
        node-role.kubernetes.io/master: ""
      restartPolicy: Always
      serviceAccountName: ovn-controller
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
      tolerations:
      - key: "node-role.kubernetes.io/master"
        operator: "Exists"
        effect: "NoSchedule"
      - key: "node.kubernetes.io/not-ready"
        operator: "Exists"
        effect: "NoSchedule"
      volumes:
      - configMap:
          name: ovn-controller-config
        name: config
